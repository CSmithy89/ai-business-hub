{
  "name": "@hyvve/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types for HYVVE Platform",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "lint": "eslint src --ext .ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "zod": "^4.1.13"
  }
}
